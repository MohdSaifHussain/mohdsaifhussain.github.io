{% extends 'base.html.j2' %}
{% block content %}
<section class="section">
  <div class="section-head">
    <h1 class="display-lg">Case ledger</h1>
    {# D-07: derived from data, never typed. #}
    <span class="mono-meta">{{ '%02d' % (projects | length) }} ENTRIES</span>
  </div>
  <p class="mono-meta">P3.1 skeleton — the scroll-snap case slider is wired in P3.2 (A3.2 in P3.3).</p>
</section>
{% endblock %}
