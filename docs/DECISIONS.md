# Technology and Approach Decisions

A living record of what this project chose, what it did not choose, and why.

**Every entry cites where the decision was actually made** (commit SHA, phase
file, or design-authority section). Entries harvested from an existing record
say so. Entries where no rationale was ever written down say *that*, and the
tradeoff is then given as a clearly marked retrospective rather than as invented
history. A decision log that manufactures reasons after the fact is worse than no
decision log, because it reads as evidence.

Each phase appends its decisions here.

Governing documents: `decisions/STEP-01-CHARTER.md` (frozen v1.0 + Amendment
Log) wins every conflict; `decisions/STEP-02-HANDOFF.md` wins every visual
question; `reference/design.html` is visual reference only and is never shipped
code.

---

## Phase P3.1: Scaffold, build pipeline, fonts, tokens, repo connection

| # | Decision | Alternative(s) not taken | Reason | Recorded in |
|---|---|---|---|---|
| 3.1.1 | **Tier FULL** for the whole build | (a) STANDARD; (b) FULL without a DECISIONS log | The build ends in a public deploy, a `v1.0.0` tag, and an /audit page making WCAG/CSP/Lighthouse claims to strangers. Charter §8 already mandates phased close, honest defect logging and numbered amendments. FULL adds this log and a rehearsed release | Director's ruling, session 2026-08-06 |
| 3.1.2 | **Global git identity set to `263689115+MohdSaifHussain@users.noreply.github.com`** before the first commit | (a) repo-local only; (b) keep the prior address and declare C-33 UNMET | Defect D-01: commit authorship is immutable once pushed, and the prior address would be permanently public. Director chose global scope deliberately, accepting the effect on other repos on this machine | Director's ruling, 2026-08-06; STEP-03 §3.1 |
| 3.1.3 | **GitHub-derived stats are version-anchored with per-count evidence links**; `projects.json` rewritten to promise exactly that | (a) run each external repo's suite in CI for genuinely live counts; (b) drop test counts entirely; (c) ship the existing "live count at build" string | Defect D-02: the GitHub API returns no test count. (a) is fragile — a broken external repo would break this site's build — and unverifiable by eye. Anchoring each count to a release tag or SHA plus a link to its source keeps the claim at the exact width of the evidence | Director's ruling, 2026-08-06. **Implemented P3.2** |
| 3.1.3a | **Deferred to v1.1:** each source repo's CI publishes a `stats.json` this site consumes at build, giving genuinely live counts without this site executing foreign test suites | Building it in v1.0 | Recorded as the forward path at the time the ruling was made, so v1.1 implements a decision rather than inventing one | Director's ruling, 2026-08-06 |
| 3.1.3b | **Recorded deviation from the frozen handoff.** Version anchors and evidence links change the truth strip (handoff §3) and the card (handoff §4) | Implementing §3/§4 literally | Charter-mandated: C-27 traceability and C-35 sourcing. Charter wins every conflict. Logged as a deviation rather than absorbed silently, per charter §8 | Director's ruling, 2026-08-06 |
| 3.1.4 | **v1.0.0 ships `VISITS —`; zero third-party resources** | Creating a GoatCounter account and wiring the counter | Defect D-03: GoatCounter publishes no stable SRI hash, so C-19 cannot fully cover it. The dash is already the designed state in handoff §3. Counter sits behind a one-line flag in `build.py` (`VISITS`) for later | Director's ruling, 2026-08-06 |
| 3.1.5 | **Charter Amendment 1** — C-33 scoped to contact-capable addresses; `users.noreply.github.com` exempt in commit metadata and in documentation of the git command | (a) checker allowlist with the exception noted only on /audit; (b) report C-33 UNMET; (c) keep the literal reading | Defect D-10: under a literal reading, committing at all makes C-33 permanently UNMET. Director's stated reason for amending over allowlisting: *the frozen text and the reported verdict must agree* — an exception noted only on /audit leaves the charter permanently contradicting its own MET claim | STEP-01-CHARTER.md, Amendment Log, Amendment 1 |
| 3.1.6 | **The ✓/✗ motif renders as a matched inline-SVG pair from one shared definition; ● is a CSS-drawn dot** | (a) substitute `×` U+00D7 for ✗; (b) SVG only for the two absent marks, ✓ stays a font glyph; (c) amend §5 to permit a third symbol face | Defect D-14: ✗ and ● exist in none of the five faces, and no X-mark variant exists in either family. Director's added condition: one definition, two marks, shared stroke-weight and size — so a future edit cannot drift one mark without the other. Enforced by the `MARK_SOURCE` and `MARK_DRIFT` gates | Director's ruling + addendum, 2026-08-06; `templates/_macros.html.j2` |
| 3.1.7 | **Both faces fetched from `google/fonts` OFL trees**, subset with fontTools 4.63.0 | IBM/plex upstream (`@ibm/plex-mono@2.5.0`) for Mono | One consistent licence tree publishing static instances at exactly the weights handoff §1 specifies. IBM/plex has moved to per-family releases and its latest release is Sans. Sources cited in `tools/subset_fonts.py`; upstream origin recorded there too | `tools/subset_fonts.py` header |
| 3.1.8 | **`font-display: swap` retained provisionally** | Dropping it now for preload-only | Handoff §6 bans it only *if it costs a Lighthouse point*, which is unmeasurable until P3.5 has the tooling. Director ruled: do not drop it blind. Carried as obligation O-8 | Director's ruling, 2026-08-06 |
| 3.1.9 | **Two numbering schemes kept:** `STEP-NN` for `docs/decisions/` files, `P3.x` for build phases, charter "Phase 3" for the whole build | Restating prior documents under one scheme | Raised as a conflict rather than guessed. Director ruled: declare the mapping once, no restatement | STEP-03 §"Numbering scheme" |
| 3.1.10 | **Output uses directory-style clean URLs** (`/projects/index.html` served as `/projects/`) | Flat `projects.html` | *Chosen by default; rationale not previously recorded.* **Retrospective:** gains clean canonical URLs for C-25 with no server config, which GitHub Pages cannot provide otherwise. Costs one extra directory level. Would choose again |
| 3.1.11 | **The colophon sentence lives in `build.py`, not in a data file** | Adding it to `profile.json` | Charter §5 fixes the sentence verbatim; it is charter-mandated site chrome, not portfolio content. C-34 governs portfolio content. Putting charter text into an owner-verified data file would trigger re-verification (O-3) for no gain. Recorded as reading R-03 | `build.py` `COLOPHON` |

### Unexamined defaults

Listed because a decision record that quietly omits its unexamined choices is a
record that flatters itself. Every tradeoff here is retrospective, written now.

| Choice | Alternative(s) | Status | Retrospective tradeoff |
|---|---|---|---|
| **Jinja2** as the template engine | Mako, string.Template, hand-rolled | Mandated in outline by charter §6A ("Python + templates"); the *choice of Jinja2 specifically* was never argued | Gains: `StrictUndefined` turns a missing data field into a build failure instead of a silent blank, which is exactly the C-34 guarantee. Autoescaping is on by default. Costs: one dependency. Would choose again |
| **sha256 truncated to 16 hex chars** for the determinism hash | Full digest; a manifest of per-file hashes | *Chosen by default; rationale not previously recorded.* | Gains: short enough for a human to compare two runs by eye, which is the actual use. Costs: not collision-proof — but this is a change-detector for the director, not a security control, and it should never be described as one |
| **`_site/` not committed** | Committing the built output | *Chosen by default.* | Gains: the deployed site is always a product of data + templates, so C-34 cannot be quietly violated by editing built HTML. Costs: the repo alone does not show what was served; the Action's artifact does |

## Phase P4.1: Signature motion (v1.1 amendment)

Design authority: `MOTION_SPEC_v1_1.md`. Contract: `STEP-08-P4.1-CONTRACT.md`.

| # | Decision | Alternative(s) not taken | Reason | Recorded in |
|---|---|---|---|---|
| 4.1.1 | **The C-10 lever's definition widens from "one variable" to "one mechanism: zeroed motion tokens, plus the animations that cannot be expressed as a token, all inside one block."** | (a) A second reduced-motion block beside the A3.6 rules; (b) a charter amendment to C-10 | The guarantee C-10 asks for is unchanged — one switch collapses all motion — and only the mechanism grew, so this is a design note and not an amendment. It is recorded because the widening is real: A3.5 is neutralised by zeroing `--motion-vt`, but A3.6 is scrubbed to scroll position and has no duration token to zero, so it is switched off by rule instead. Alternative (a) was refused twice over: a lever split across two blocks is two levers, and `check_animations.reduced_motion_body()` reads only the FIRST match, so a second block would be invisible to the gate that polices the condition | `MOTION_SPEC_v1_1.md` §A3.5; `tokens.css` lever block; owner ruling condition (2), 2026-08-10 |
| 4.1.2 | **Element selectors live in `tokens.css`, inside the lever block** | Keeping all element rules in `site.css` | A deliberate layering exception (reading R-14), taken only because 4.1.1's single-block requirement leaves nowhere else to put A3.6's kill rule. Recorded rather than left as an oddity for a reader to trip over | `tokens.css`; STEP-08 requirement 3.5 |
| 4.1.3 | **A3.5's duration is `var(--motion-vt)`, not the `250ms` literal the spec's code block shows** | Copying the spec's code block verbatim | The spec's own prose requires the token "so the lever below reaches it", which contradicts its illustrative code. Prose governs (reading R-13): the literal would put A3.5's duration permanently beyond the C-10 lever, which is the thing the surrounding paragraph exists to guarantee | `site.css` A3.5 block; `MOTION_SPEC_v1_1.md` §A3.5 |
| 4.1.4 | **`duration_type` is a closed enum of `ms \| scrubbed \| none`** | Two values, `ms \| scrubbed`, as the ruling literally specified | A3.2 is native scrolling with no duration at all. Typing it `ms` would attach a millisecond kind to something that has none, which is the invented value the same ruling forbids. Confirmed by the director at the P4.1 review stop: "each meaning what it says" | Owner ruling, P4.1 review stop, 2026-08-10; `test_duration_type_enum_is_closed` |
| 4.1.5 | **`check_reduced_motion` extended to inspect keyframe animations and `::view-transition-*`** | Leaving the two D-48 gates untouched and declaring the gap as an honest limit | The gate was proved BLIND by execution, not by argument: deleting `--motion-vt: 0ms` left A3.5 running at full 250ms under reduced motion while the checker printed "motion is switchable off (C-10)" and exited 0. Authorised by the director under condition (3)'s spirit — "the fence forbids weakening a gate to pass, not deepening one that passed vacuously." That exact sabotage is now the gate's permanent negative control | Owner ruling, P4.1 review stop; `check_animations.py` POISONED FIXTURE control |
| 4.1.6 | **A3.6's declaration lands in its own commit with its CSS, rather than the full six-item list landing with A3.5** | Literal compliance: declaring all six in the D2/D3 commit | Declaring an animation before its CSS exists makes the gate red — correctly, since that is the `UNSHIPPED_ANIMATION` direction working. Literal compliance would have forced D3 and D4 into one commit and deleted the review stop the same ruling ordered. The protected invariant is "the gate never lags its list at any commit", which this sequencing preserves at every commit. Deviation confirmed by the director at the review stop | Owner ruling, P4.1 review stop; commits `434c0b7`, this phase |

## Phase P4.2: Light mode (closing R-11 / A4.8)

Derivation and solved values: `STEP-09-P4.2-LIGHT-DERIVATION.md`.

| # | Decision | Alternative(s) not taken | Reason | Recorded in |
|---|---|---|---|---|
| 4.2.1 | **The light accent honours the 7:1 floor, which makes it a bronze `#695222` rather than the brand amber** | (a) AA-only at `#8d6e2e`, declaring 7:1 unmet for that one token; (b) the accent stops carrying text in light and links use `--ink` with an underline | No amber survives a light ground: `#e8b64c` has relative luminance 0.511 and the ceiling for AA *alone* against off-white is 0.183 — it misses by nearly 3×. The owner ruled for legibility over hue. A dimmer light background does NOT help, and this is the wrong intuition worth recording: for dark-on-light the ratio is `(L_bg+0.05)/(L_fg+0.05)`, so lowering the background lowers the ratio | Owner's ruling, 2026-08-10; `check_contrast.py` A5 rows |
| 4.2.2 | **The toggle is session-only. Nothing is stored** | (a) `localStorage` plus a render-blocking head script; (b) `localStorage` applied after paint | C-21 permits it — a theme preference is not personal data — so this was a cost decision, not a compliance one, and A4.10 says so rather than implying the charter forbade it. Applying a stored theme before first paint needs an inline script (refused by INLINE_SCRIPT and by `script-src 'self'`) or a blocking request on a page whose LCP is already UNMET. Option (b) was refused outright: a visible flash of the wrong theme on load is indistinguishable from a bug | Owner's ruling, 2026-08-10; `static/js/theme.js`; A4.10 |
| 4.2.3 | **Every hex appears exactly once; the semantic tokens are `var()` pointers remapped per theme** | A second full `:root` inside the `prefers-color-scheme` query | The obvious form would state each light hex twice — once for the media query, once for the `[data-theme]` override — and let the two drift silently, shipping one light mode to visitors whose OS says light and another to visitors who pressed the button. Nobody would see both. It also makes the palettes machine-readable without parsing CSS scopes, which is what stops `check_contrast.py` collapsing them | `tokens.css`; `test_both_light_mappings_are_identical` |
| 4.2.4 | **The browser audit records the rendered background per run and refuses if the themes match** | Trusting the `color_scheme=` argument and labelling the second run "light" | Requirement (4) asked the job to *exercise* both themes, not assume the second. Had emulation silently failed, every run would have been dark, every run would have passed, and the light half of the audit would have been dark pages wearing a label — the D-48 disease. `THEME_NOT_APPLIED` is the reason code | `browser_audit.py`; owner's requirement (4) |
| 4.2.5 | **The theme toggle is styled as a control, not a nav item — no number prefix** | Adding it to `.nav-list` as a sixth entry alongside `01`–`04` and `A` | The `01`–`04` + `A` numbering means *this is a place you can go*. The toggle is a thing you press, and numbering it would make the wayfinding scheme lie about what it indexes. It follows `.pdf-button`, the site's existing language for a control, and reuses the inverted accent pair already measured in both themes. Recorded as reading R-17 | Owner's condition, 2026-08-10; `site.css` `.theme-toggle` |

| 4.2.6 | **Light-mode text values stay exactly as shipped** | Darkening `--body` / `--bright` further for more contrast headroom | Considered at the owner's live eye check and decided against, recorded so the question does not resurface. Light `--body` measures 12.16:1 and is comfortably readable; further darkening trades toward halation and eye-strain on a bright ground for marginal measured gain. Contrast is not a quantity to maximise — it is a floor to clear, and this clears it with room | Owner's ruling at the live check, 2026-08-10 |
| 4.2.7 | **Theme persistence is `sessionStorage`, applied before paint by a render-blocking external script** | (a) no storage, per-page, as first shipped; (b) `localStorage`, persisting across visits; (c) storage applied after paint | (a) was the defect D-59 records — it under-implemented the recorded per-visit ruling. (b) outlives a visit and says more about the visitor than the ruling asked for. (c) shows a visible flash of the wrong theme, indistinguishable from a bug. The cost is one small request on a critical path whose LCP is already UNMET, so the standing measurement ran with the revert rule armed: LCP worst-median over 1.55 s would have reverted persistence and restated A4.10 as per-page | Owner's ruling, 2026-08-10; `theme-init.js`; D-59 |

| 4.2.8 | **No README. The earlier ruling is REAFFIRMED at the v1.1.0 release and the question is closed** | Adding a README with a project description, badges and a quickstart | The repository's landing surface is the published site, and the site's landing surface is /audit — which already states what this is, how it was built, what it measures, and what it fails. A README would be a fourth telling of the same thing, in the one place with no gate over it: no checker reads a README, so it is the file most free to drift from the truth the rest of the repository is forced into. The bare-ledger repo stands. `CHANGELOG.md` was added at v1.1.0 because it carries something no other file does — what changed between two releases — and every figure in it is sourced from the record. Reaffirmed by the owner, 2026-08-10, so the question does not resurface | Owner's ruling, 2026-08-10 |

### Reversals

| What changed | From | To | Trigger |
|---|---|---|---|
| C-33's scope | Literal: no email address anywhere, unqualified | Contact-capable addresses only; noreply exempt in two named places | Defect D-10, found by reading before the first commit |
| `projects.json` count semantics | "live count fetched at build" | Version-anchored to a release/SHA, with an evidence link per count | Defect D-02, found by reading the data against what the GitHub API actually returns |
| Body-text floor in the shipped CSS | 12px, as `reference/design.html` renders it in 21 places | 12.5px, per handoff §1's own stated minimum | Defect D-12, found by tallying font declarations in the reference |

## Phase P5.1: The card's basis sentence, per entry (STEP-10)

Contract: `STEP-10-P5.1-CARD-BASIS-CONTRACT.md`. Tier STANDARD, the first phase of this site not run at FULL; chosen because the deploy path was exercised twice the same day and the owner's local read is the verification that matters.

| # | Decision | Alternative(s) not taken | Reason | Recorded in |
|---|---|---|---|---|
| 5.1.1 | **The basis sentence lives in `build.py` as `BASIS_SENTENCES`, one per basis, and the template prints `p._basis_sentence`; a basis with no sentence refuses the build, with `BASIS_RETIRED` distinct from `BASIS_UNKNOWN`** | (a) Four `{% if %}` branches in the template; (b) sentences in `projects.json` as data; (c) a silent fallback to the resume sentence | (a) puts meaning in a template with no reason code and nothing to unit-test. (b) would put site chrome into an owner-verified data file, the same objection as 3.1.11 for the colophon. (c) is exactly the defect. Wordings are the owner's ruling, 2026-08-22 | Owner's ruling, 2026-08-22; `build.py`, `tests/test_card_basis.py`; D-60 |

## Phase P5.2: The visit counter retired (STEP-11)

Contract: `STEP-11-P5.2-VISITS-RETIRED-CONTRACT.md`. Tier STANDARD.

| # | Decision | Alternative(s) not taken | Reason | Recorded in |
|---|---|---|---|---|
| 5.2.1 | **The `VISITS` element is removed from the footer; A4.4 moves to the resolved register; no counter replaces it.** A recorded deviation from handoff §3, which designed the dash | (a) Keep the dash, as 3.1.4 ruled for v1.0.0; (c) wire a counter and narrow the zero-third-party claim; (d) read traffic from the repository's Insights only | The owner read the live page and asked whether the counter was broken: a designed dash that reads as a broken feature is not the honest state 3.1.4 intended. (c) trades a standing claim for a number that starts at zero. (d) is what the owner does anyway and needs no element | Owner's ruling, 2026-08-22; `templates/base.html.j2`, `tests/test_visits_retired.py` |

## Phase P5.3: Mobile legibility, the robots audit, supply-chain pins (STEP-12)

Contract: `STEP-12-P5.3-MOBILE-LEGIBILITY-SECURITY-CONTRACT.md`. Tier STANDARD. Eye-check waived by the owner for this batch.

| # | Decision | Alternative(s) not taken | Reason | Recorded in |
|---|---|---|---|---|
| 5.3.1 | **The three small mono type steps are 12px at or below 900px; desktop keeps the handoff scale** | (a) Raise the tokens globally; (b) accept best-practices 96 as declared | Lighthouse's legibility line is a mobile-profile audit and desktop already measured 100; (a) changes a design the owner approved for no measured gain. (b) leaves a fixable gap declared, which the register exists to avoid when the fix costs nothing the charter protects | `static/css/tokens.css`; `tests/test_p53.py` |
| 5.3.2 | **CSP `connect-src 'self'`, not `'none'`** | Keep `'none'` and declare SEO 92 permanently; or drop robots.txt | The 92 was never about robots.txt: Lighthouse fetches it from inside the page and `'none'` blocked the fetch on all 30 runs (D-61). `'self'` forbids exactly what `'none'` was guarding against, a beacon to another origin, and `test_no_third_party_resources_load` still proves nothing else loads | `build.py` `CSP`; D-61 |
| 5.3.3 | **Every third-party action is pinned by commit SHA, version kept as a comment; CodeQL added for python and actions** | Tag pins as before (a tag can be moved) | A moved tag is the supply-chain failure the skill's own rule 3 warns about; Dependabot's github-actions ecosystem already bumps SHAs, so the cost is one-time. CodeQL covers the only code this repo executes: the build tooling and the workflows | `.github/workflows/*.yml`, `codeql.yml` |
| 5.3.4 | **C-02 (LCP 1.53 s vs 1.5 s) stays declared; no LCP phase** | Inline `theme-init.js` in `<head>` under a CSP `'sha256-…'` hash | C-18 forbids inline script and the CSP carries no hashes by design (`gate_inline`, `INLINE_SCRIPT`); the render-blocking external script is ruling 4.2.7's accepted cost. Manufacturing a change against a standing rule to win 30 ms would be the overclaim rule 9 names. If C-18 is ever re-ruled, this is the first lever to try | This row; CHANGELOG "Declared, unchanged" |
| 5.3.5 | **Dependabot merges are released by the workflow polling the PR's own check runs, then dispatching the deploy; a SHA-only Actions bump merges only when the new SHA is what the version comment's upstream tag peels to** | (a) Enable allow-auto-merge and protect `main` with required checks, keeping `--auto`: needs repository settings this record cannot show or test, and required checks on a PR are still only CodeQL. (b) Merge SHA-only bumps unconditionally: a moved SHA under an unchanged version is the exact signal 5.3.3 pins for. (c) Hold every SHA-only bump for the owner: turns the pin correction D-62 makes necessary into a weekly chore | The verification is against the upstream repository by API at decision time, with both SHAs printed on refusal; the wall still stands between the merge and the site because the deploy publishes only on a passing build. Owner's direction 2026-08-25: automate the Dependabot handling | Owner's direction, 2026-08-25; `.github/workflows/dependabot-auto-merge.yml`; D-62, D-63 |
| 5.3.6 | **Wrapped link rows get a 24px minimum tap box on viewports at or below 900px; desktop unchanged** | (a) Drop finding-bridge to four links so the row fits one line: hides a latent defect behind content, and the next entry with five links measures it again. (b) Raise `--type-mono-link` line-height globally: changes the approved desktop design for a mobile-only failure, the 5.3.1 objection. (c) Declare accessibility 95 on /audit: the register is for what cannot be fixed, and this can | The box grows, the type does not; the fix is scoped to the two rows that wrap and the viewport where they wrap, and Lighthouse confirms it locally on both pages before the deploy | `static/css/site.css`; D-64 |

## Phase P5.4: Architecture diagrams on /projects/ (v1.2)

Owner's direction, 2026-08-27: a graphical upgrade to the Projects page. Two directions were tested in a local build before this one and rejected by the owner on sight, recorded here so they are not proposed again: (a) a site-wide scroll-reveal on the existing layout, judged invisible ("there is no difference"); (b) full-viewport "scene" sections per entry with snap scrolling and a word-by-word hero, judged "too much waste of space", which is the honest result of putting short text in a full screen with nothing else to fill it. The lesson, stated so it is not relearned: on a typography-only site, motion has nothing to choreograph until there is visual material; the material has to come first.

| # | Decision | Alternative(s) not taken | Reason | Recorded in |
|---|---|---|---|---|
| 5.4.1 | **Each live project entry carries its own architecture drawing, transcribed from that project's README, as inline SVG that posts itself (A3.7) when it reaches the reading edge; the archived FDE case study has none** | (a) Raster or external images: forbidden by C-19 and C-05's posture, and unreadable at phone width. (b) A generic illustration per card: decoration, which C-12's one-sentence test refuses. (c) Embed the READMEs' mermaid via a library: a third-party script on the published surface | The drawings are the projects' own content, quoted from their READMEs with the line numbers in `projects.json` `diagram_source`; about 5 KB of SVG each, zero images, zero libraries | `tools/gen_diagrams.py`, `templates/diagrams/`, `data/projects.json` |
| 5.4.2 | **Drawings are generated by `tools/gen_diagrams.py`, which refuses to write one in which any text overlaps a node or another text (`REASON=DIAGRAM_OVERLAP`); the committed files must be byte-identical to the generator's output** | Hand-authored SVG | The owner's first look at the mock found a label starting in one box and ending in the next. A hand edit can reintroduce that silently; a generator with a collision check cannot, and `tests/test_diagrams.py` carries a negative control proving the check trips | `tests/test_diagrams.py` |
| 5.4.3 | **Edges are straight lines drawn by `transform: scale()` from their start; nodes rise and fade. C-15 is therefore NOT amended** | `stroke-dashoffset` line drawing, which would need C-15 widened | The same visual result inside the existing rule; the gate that enforces C-15 already covers it | `static/css/site.css` `@ships A3.7` |
| 5.4.4 | **Zoom controls (minus, plus, RESET) scale the drawing inside its own natively scrollable frame; reserved in the layout and revealed by JS with `visibility`, never `display`** | A lightbox; pinch-only | The owner asked for a zoom "like the README's". A lightbox is a new surface and a new animation; scaling inside the existing frame is native scrolling (C-14) and cannot shift the page (D-58) | `static/js/diagram.js` |
| 5.4.5 | **Only the diagrams animate. The site-wide reveal built in the same session is withdrawn; A3.1 to A3.6 are unchanged** | Keep the reveal alongside | Owner's ruling: "we will skip the animations upgrade, we only do this upgrade today" | `data/audit-spec.json` A3.7 |

## Phase P5.5: Newest push first, with the date (v1.2)

Owner's direction, 2026-08-28: entries in order of latest update, automatically, on Projects and Home, with the push date shown; "highest standards" asked for and answered honestly. There is no standard that orders a portfolio. What applies, and is applied, is: ISO 8601 for the date and HTML's `<time datetime>` for its machine form; Schema.org `dateModified` in the existing ItemList JSON-LD so structured data and the visible page agree; reverse-chronological order as the convention of every professional record (Keep a Changelog, Atom/RSS, GitHub's own repository lists); WCAG 2.2 AA for the rendering, already gated.

Sources, fetched 2026-08-28 rather than recalled (doctrine rule 3), with what each was checked for:
- WHATWG HTML Living Standard, the `time` element, https://html.spec.whatwg.org/multipage/text-level-semantics.html#the-time-element : a valid global date and time string (`2011-11-18T14:54:39Z` in its examples) is permitted in `datetime`, and a valid date string (`2011-11-18`) as content.
- Schema.org `dateModified`, https://schema.org/dateModified : "The date on which the CreativeWork was most recently modified", expected type Date or DateTime, on CreativeWork. Schema.org `SoftwareSourceCode`, https://schema.org/SoftwareSourceCode : Thing > CreativeWork > SoftwareSourceCode, so it inherits `dateModified` and `version`, and defines `codeRepository` and `programmingLanguage`, the four properties this site's JSON-LD uses.
- Keep a Changelog 1.1.0, https://keepachangelog.com/en/1.1.0/ : "The latest version comes first" and dates in ISO 8601 `YYYY-MM-DD`, the two conventions applied to the entry order and the visible date.
- GitHub REST API, Get a repository, https://docs.github.com/en/rest/repos/repos#get-a-repository : `pushed_at` is "string or null, format: date-time", the source of every date shown. The page names API version `2026-03-10`; `tools/fetch_stats.py` sends no version header and so receives GitHub's default, which is a follow-up to pin, not a change made blind here.
- WCAG 2.2, https://www.w3.org/TR/WCAG22/ : W3C Recommendation, 12 December 2024, the current version; the browser audit already gates AA on every page in both themes.

| # | Decision | Alternative(s) not taken | Reason | Recorded in |
|---|---|---|---|---|
| 5.5.1 | **Entries render newest `pushed_at` first on every page, sorted at build time by `build.order_by_push`; `projects.json` keeps its owner-verified order untouched** | (a) Reorder the data file by hand on each addition: a habit, and the D-60 shape (a rule that holds until nobody remembers it). (b) A typed `date` per entry: a claim with no source | The date is GitHub's own, fetched by `fetch_stats.py` and already committed in the snapshot; the build refuses an entry without one (`PUSH_DATE_MISSING`) rather than placing it | `build.py`, `tests/test_order.py` |
| 5.5.2 | **The visible date is the ISO date (`PUSHED 2026-08-25`), inside `<time datetime>` carrying the full UTC instant; the same instant is the JSON-LD `dateModified`** | A relative "3 days ago" (needs JS or goes stale); a locale format (ambiguous 08/25 vs 25/08) | ISO 8601 is unambiguous in every locale and needs no script; a test proves the visible date, the machine date and the structured-data date agree | `templates/_macros.html.j2` `pushed`, `build.py` |
| 5.5.3 | **The order is as current as the snapshot, and the page says so: refreshed every Monday by `refresh-stats.yml` and on every content update; declared as A4.11** | Fetch at build (contract 3.2 forbids it); fetch in the browser (C-35 forbids it) | An honest limit stated beats a live claim the build cannot make | `data/audit-spec.json` A4.11 |

## Phase P5.6: The record block, ISSUED and MODIFIED (v1.2)

Owner's direction, 2026-08-28: two dates per entry, the latest official release and the last automated update, in a dedicated block, named to a standard. The names are DCMI Metadata Terms, fetched 2026-08-28 (https://www.dublincore.org/specifications/dublin-core/dcmi-terms/): **issued**, "Date of formal issuance of the resource"; **modified**, "Date on which the resource was changed". Structured data: Schema.org `datePublished` ("Date of first publication", https://schema.org/datePublished) for the version's issuance and `dateModified` for the push. Version text follows SemVer 2.0.0 with the conventional `v` prefix, which the spec's FAQ notes is not part of the version but the usual way to mark one (https://semver.org/spec/v2.0.0.html). The date source is GitHub's REST API, Get the latest release (https://docs.github.com/en/rest/releases/releases#get-the-latest-release): `published_at` is when the release was made public, `created_at` the commit's date, and "latest" is the most recent non-prerelease, non-draft release sorted by `created_at`.

| # | Decision | Alternative(s) not taken | Reason | Recorded in |
|---|---|---|---|---|
| 5.6.1 | **`tools/fetch_stats.py` pins `X-GitHub-Api-Version: 2026-03-10`** | Stay on the unstated default (2022-11-28, supported to 2028-03-10) | The 2026-03-10 breaking-changes list (https://docs.github.com/en/rest/about-the-rest-api/breaking-changes) was read against every field the tool uses; none is affected, and a pinned version changes shape only when this repository changes it. The first pinned fetch was diffed field by field against the previous snapshot before commit: identical apart from the new `issued_at` keys and the `as_of` stamp | `tools/fetch_stats.py` |
| 5.6.2 | **ISSUED is the anchored version's issuance: `published_at` for a release, the tag commit's committer date for a bare tag, and none for a commit anchor, which renders "no release yet"** | Borrow the push date for never-released repositories; use `created_at` | A push is not an issuance, and `created_at` is the commit's date, not the publication's; both would be the D-02 shape, a date with the wrong source | `tools/fetch_stats.py` `anchor_for`, `build.py` `order_by_push` |
| 5.6.3 | **The block is a `<dl>` with two rows, ISSUED (version linked, then its date) and MODIFIED (date), on every Projects card and Home flagship row; a one-sentence legend on Projects names both terms and the snapshot's "as of"** | Two more items in the links row | A definition list is the element whose meaning is "terms and their values"; two dates in a row of links read as two more links | `templates/_macros.html.j2` `record` |
| 5.6.4 | **Order stays newest MODIFIED first (5.5.1)** | Newest ISSUED first | The owner asked for "latest update first"; a release is a milestone, a push is the update | `build.py` |

## Phase P5.7: Light mode re-derived, and a high-contrast layer (v1.2)

Owner's observation, 2026-08-28: in light mode the accent "is not dark enough and the words are too slim"; the dark theme is to be the base of readability. Both halves are real. The light accent measured 7.05:1 against the dark accent's 10.40:1, and dark-on-light text is rendered visibly thinner than the same weight reversed, which WCAG's own guidance names. Sources, fetched 2026-08-28 rather than recalled (doctrine rule 3):

- WCAG 2.2 Understanding 1.4.3, https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html : "thin or unusual fonts may be rendered by user agents with a much fainter color"; authors should "choose a font with stronger/thicker lines, or aim for a foreground/background color combination that exceeds the normative requirements". This is the authority for the Medium weight and the deeper light values.
- WCAG 2.2 Understanding 1.4.6 (Enhanced, AAA), https://www.w3.org/WAI/WCAG22/Understanding/contrast-enhanced.html : 7:1 normal text, 4.5:1 large text (18pt, or 14pt bold), chosen for roughly 20/80 acuity. This is the target the high-contrast palettes must reach on every pair.
- MDN `prefers-contrast`, https://developer.mozilla.org/en-US/docs/Web/CSS/@media/prefers-contrast : values `no-preference`, `more`, `less`, `custom`; driven by the operating system's setting; Baseline widely available since May 2022.
- MDN `forced-colors`, https://developer.mozilla.org/en-US/docs/Web/CSS/@media/forced-colors : Windows High Contrast replaces author colours with system colours and drops backgrounds; authors make small targeted adjustments with system colour keywords.
- WCAG 3.0, https://www.w3.org/TR/wcag-3.0/ : W3C Working Draft, 3 March 2026; its text-contrast method is still being developed. This site therefore claims nothing beyond WCAG 2.2, and does not cite APCA as a standard.

| # | Decision | Alternative(s) not taken | Reason | Recorded in |
|---|---|---|---|---|
| 5.7.1 | **Light palette re-derived with dark as the base: accent `#4f3c14` (10.03:1 against dark's 10.40), body `#2b2a28` (13.62:1), dim `#57554f` (7.08:1); dark dim raised to `#9f9c92` (7.08:1). Every text pair in both colour themes now reaches 7:1; the site's one declared AAA shortfall (A1.3, --dim) is closed** | Keep the STEP-09 derivation (ratio parity only) | Ratio parity produced the thinner, browner light mode the owner saw; the standard's own guidance is to exceed the numbers where rendering is thin. Every value re-derived by `check_contrast.py` on every build; STEP-09's solved values are superseded by this table, its rule (light introduces no new hue) kept | `static/css/tokens.css`, `tools/check_contrast.py` |
| 5.7.2 | **Mono text weight is a token, `--w-mono`: 400 in dark, 500 (Plex Mono Medium, already self-hosted) in light and in high contrast. Link and label weights unchanged** | Darker colour only | The face is monospaced, so the weight change reflows nothing, costs no bytes, and is the remedy 1.4.3's guidance names first | `static/css/tokens.css` |
| 5.7.3 | **A high-contrast layer, reached by the OS setting (`prefers-contrast: more`) and by a visible "High contrast" toggle beside "Light mode"; the manual choice wins, per visit, in sessionStorage, the theme toggle's own pattern (4.2.x, D-59). It raises body to the bright step, dim to 7.70 / 8.03:1, the light accent to 12.15:1, rules to .7 / .65 alpha, and mono weight to 500. `check_contrast.py` evaluates the two HC palettes as palettes and refuses `HC_BELOW_AAA`** | (a) OS setting only, no button: honours the standard but hides the capability from anyone who has not found the OS setting. (b) An accessibility overlay widget (font sliders, colour filters, a floating menu): a third-party script on the published surface (C-19), and it duplicates what the OS and the browser already do better. (c) A dyslexia-font option: no self-hosted face exists for it and the evidence base is contested | A toggle that maps to a real media feature is the same design as the theme toggle, not a widget; the OS path means users who set "increase contrast" get it without touching the site | `static/css/tokens.css`, `static/js/theme.js`, `static/js/theme-init.js`, `templates/base.html.j2` |
| 5.7.4 | **Forced colours (Windows High Contrast) get three targeted rules: the live dot becomes a currentColor ring, a pressed toggle shows an outline, diagram shapes fill with `Canvas`** | Nothing | These are the only elements whose meaning lives in a background colour, which forced colours remove | `static/css/site.css` |
| 5.7.5 | **The browser audit still runs axe on the two colour themes; the high-contrast palettes are verified by `check_contrast.py`'s WCAG 2.2 computation, which is the same formula axe's colour-contrast rule applies** | Run axe four times per page | The witness logic in `browser_audit.py` is built around the two colour themes; doubling the matrix for a computed guarantee already held is cost without a new fact. Declared, not hidden | this row |

## Phase P5.8: The Case studies section (v1.2)

Owner's direction, 2026-08-28: on /projects/ only, a second section below the projects, titled Case studies, holding the two engagement records, each carrying the result of its case study where a project carries its architecture.

| # | Decision | Alternative(s) not taken | Reason | Recorded in |
|---|---|---|---|---|
| 5.8.1 | **An entry flagged `case_study` renders in the Case studies section of /projects/ and nowhere else; the same card, gates, basis sentence, record block and newest-MODIFIED-first order apply. The FDE engagement moves there; ts-sentry-case-study is added, every field from its README at origin/main 6b09456** | A separate page (a sixth nav item): the owner ruled this page only. Mixing them into the projects slider: a record of an engagement is not a tool, and the reader should not have to tell them apart by reading | One schema, one macro, two sections; the count line and JSON-LD stay derived | `data/projects.json`, `build.py`, `templates/projects.html.j2` |
| 5.8.2 | **The outcome block is a definition list, "OUTCOME, FROM THE README": the README's own row labels as terms and its cells or lead sentences as values, grouped under the README's own headings, with the lines cited beneath. Verbatim, by the owner's instruction ("no paraphrasing, all the content from the disk or repo only"): the READMEs' own em-dashes, en-dashes, arrows and ≤ are kept, which supersedes CU-4's habit of dropping em-dashes for this section (the C-27 gate is scoped to experience.json, CU-2). Only backticks, bold and links are dropped** | Summarise the results in the site's voice | The owner's rule for entry text is the repository's own words, and a result restated is a result changed. `test_every_case_study_carries_its_outcome_and_source` proves every term and value renders | `data/projects.json` `outcome` |
| 5.8.3 | **Home is untouched: its truth strip still lists every entry with a figure, and the flagship rows are unchanged** | Drop case studies from the strip | The owner's ruling: this change is /projects/ only | `templates/index.html.j2` (`entries`) |

## Phase P5.9: prevalence-kit, and the figure kind (v1.2)

Owner's direction, 2026-09-01: add prevalence-kit, and "instead of a graph I have an image in the README, please upload that image instead of the graph". Ruled FULL tier, image replaces the architecture diagram, flagship. Ruled again the same day, on how the image is adapted: mechanical recolour, "make sure you take the image first, check how you can display the image according to three modes light dark and high contrast and come to a final conclusion and then do it", and then "make sure once you update please check if its working on all type of devices and screens".

| # | Decision | Alternative(s) not taken | Reason | Recorded in |
|---|---|---|---|---|
| 5.9.1 | **prevalence-kit added as the eighth entry and fifth flagship, REPO-STATED, 2026-09-01.** Source: README.md at origin/main 6155194, byte-identical to the working tree at C:\Users\mohds\prevalence-kit (clean, `git status`; HEAD == origin/main). Problem :13, :15-17 and :19; method :47-48, :50-51, :62-63, the six-verb table :69-74, and :59-60; metrics :79-80 and the badge labels :6, :7, :8, plus :234 and :206; collaboration :222-223 and the Honest limits closing bullet. Markup, links, code ticks and bold dropped; table cells joined into sentences; no word changed. Anchor: release v1.0.0 via `tools/fetch_stats.py` (issued 2026-08-31T19:21:26Z, pushed 2026-08-31T19:48:11Z), so it renders first by P5.5's newest-push order. Every link answered HTTP 200 on 2026-09-01 and `--verify-links` exits 0 | Summarise the README in the site's voice: forbidden by the standing instruction (CU-5) | REPO-STATED is the basis whose definition (CU-1) matches a README-published figure at an anchored ref | `data/projects.json` |
| 5.9.2 | **A live entry may carry `figure` instead of `diagram`: the figure its own README publishes, adapted by `tools/gen_chart.py`. It is INLINE SVG, not a served file** | (b) `<img src="/assets/img/...svg">`: an image document cannot resolve the host page's custom properties, so it could never follow the theme and would be a white slab in dark mode — which fails the owner's own condition for this change. (c) Regenerate the chart in the site's drawing style: a redraw, which is what the owner ruled against | The owner's condition was that it work in light, dark and high contrast. Inline is the only construction that can. The invariant in `_status` widens from "every live entry carries a diagram" to "one of the two", and `test_every_live_entry_carries_a_figure_or_a_diagram` holds the widened form | `tools/gen_chart.py`, `templates/figures/`, `templates/projects.html.j2` |
| 5.9.3 | **The adaptation is mechanical and the mechanism is the evidence.** Eight colour attributes become classes; the opaque white ground is deleted so the page's own background shows through; the root font-family goes. `tests/test_chart.py` re-derives every element, coordinate, path and word from the committed source with a parser of its own and compares, so "only the colours changed" is a checked fact rather than a claim | Hand-editing the SVG | A retyped drawing cannot be diffed against its origin. The source is committed at `reference/prevalence-kit-coverage-source.svg` with its sha256 pinned in the generator, which refuses if it moves | `tools/gen_chart.py`, `tests/test_chart.py` |
| 5.9.4 | **The four lines of prose leave the drawing and render as HTML beside it.** Measured in a browser, 2026-09-01, against the 760-unit box: in the source's own Georgia one line overruns (the long footnote, ending at 952.6, over by 192.6); in this site's monospace three do (subtitle over by 45.4, second footnote by 90.4, long footnote by **436.0**). The root `<svg>` clips to its viewport, so an overrun is lost text, not just ugly text. **Stated in that direction on purpose: this site's wider monospace widened an inherited problem from one line to three, and the correction moves against us.** A first pass in this session put all three overruns on the source by applying the monospace advance of 0.6 em to Georgia as well; that arithmetic was wrong, the browser is the correction, and the original figure is kept here beside it rather than quietly replaced. The title fits in both fonts and was moved with its subtitle for coherence, not because it clipped. The viewBox is then cropped to `0 42 760 386`, computed from the retained geometry, because that space existed only to hold the prose | (b) Widen the viewBox to about 1,216 units: the plot would shrink to a strip. (c) Shrink the footnotes to roughly 7 units: illegible | SVG text cannot wrap. HTML text does, at every viewport, and is selectable and read as text. The words and their order are the source's, and `test_figure_prose_is_the_sources_own_words` refuses a one-character drift | `tools/gen_chart.py`, `data/projects.json` `figure` |
| 5.9.11 | **The entry opens with the README's own opening, on the owner's direction of 2026-09-01.** `problem` becomes README :13, :15-17 and :19 — "Audit-grade prevalence measurement for Trust & Safety … No AI ever touches the evidence or the estimate." Those sentences previously OPENED `method`, so they are removed from it: leaving both would print the same paragraph twice on one card. The gap sentence they displace (:62-63) moves into `method`, and takes with it the antecedent the README gives it (:50-51, "It is not a survey-statistics library, and svy is the estimator layer"), because "what neither library does" names nothing on its own once lifted out of the comparison it sat in | (b) Keep the gap sentence as `problem` and open `method` differently: the owner's direction was specific about which words open the entry. (c) Drop the gap sentence entirely: it is the clearest statement of what the tool is for, and losing it to a reshuffle would be a quiet edit | The owner directs the entry text and the README supplies the words; both hold here. **A first attempt at this instruction pasted finding-bridge's `problem` verbatim — a sentence naming finding-bridge twice — which would have described prevalence-kit as a red-team finding normaliser and printed one paragraph twice on /projects/ under two names. Raised rather than applied, and the owner corrected the paste.** Recorded because the near-miss is the lesson: entry text is checked against the entry it belongs to, not just pasted | Owner's direction, 2026-09-01; `data/projects.json` |
| 5.9.10 | **Finding, inherited and widened, then ruled: the Clopper-Pearson line crossed its own legend label.** Measured by sampling the path against the label's bounding box: in the source, with Georgia, 14 crossings over x 192.7 to 224.1; here, with the wider monospace, 25 over x 185.4 to 243.5. The Wilson label was clipped at its corner in both (4 there, 6 here). **This is the source drawing's own defect, not one this phase introduced — and this phase made it longer**, said plainly rather than filed under "as the README has it". **The owner confirmed it by eye on 2026-09-01 and ruled that the labels be placed "how labels are professionally positioned in such images". The legend therefore moves OUT of the plot onto one horizontal row beneath the axis** (swatches and labels at y=444/448, entries spanning x=274 to 537, centred on the plot's own span of 80 to 730). That is the published convention — matplotlib's `loc="lower center"` outside the axes, ggplot2's `legend.position = "bottom"` — and it is collision-free by construction rather than by finding a gap and trusting nothing grows into it. The viewBox extends to `0 42 760 422`, computed from the retained geometry as before. Annotation only: no data point, no axis, no gridline, no word | (b) **Direct labelling at each line's end**, the Cleveland and Tufte preference and otherwise the better answer: measured and it does not fit — both series end at x=648.8 and "Clopper-Pearson" is 117 units wide, so it runs past the 760-unit box. Recorded because it was the first choice, not because it won. (c) An earlier pass placed the legend inside the plot's empty band at x=300, y=248: superseded by the owner's ruling, and it was the weaker answer anyway, since "empty" is a fact about today's data. (d) Halo the label under `paint-order`: moves no coordinate but adds paint the source never had, and leaves the reader's eye still travelling between key and line | A legend outside the data region cannot be crossed by the data. The move is exempted BY NAME in `test_every_coordinate_and_word_survives_the_recolour` — pulled out of both sides and counted at exactly four — so the exemption cannot quietly grow, and two further tests hold it on one row, outside the plot, and centred | `tools/gen_chart.py`, `tests/test_chart.py`; owner's ruling 2026-09-01 |
| 5.9.5 | **The Wilson series is dashed as well as coloured.** Under `forced-colors: active` the browser repaints both series to one system colour, so a hue-only distinction leaves the legend as two identical swatches with two different labels. The dash reaches the legend swatch, not only the data path, and its pattern is deliberately not the nominal line's own `6 4` | Colour alone, as the source has it | WCAG 2.2 SC 1.4.1, <https://www.w3.org/WAI/WCAG22/Understanding/use-of-color.html> (fetched 2026-09-01). This is the one place the adaptation adds rather than substitutes, and it is declared as a deviation in STEP-13 §6 | `static/css/site.css`, `tests/test_chart.py` |
| 5.9.6 | **The figure does not animate.** It is classed `.chart`, and `diagram.js` selects `.diagram`, so it is never armed | Reusing `.diagram` and its A3.7 posting | A3.7's declared animation is nodes rising and edges drawing; a scatter chart posting would be a different animation under a declared name. Nothing new ships, so nothing new is declared, and `check_animations.py` stays green in both directions | `static/css/site.css`, `templates/projects.html.j2` |
| 5.9.7 | **The PyPI link was dropped, not the fact.** `test_outbound_links_are_allowlisted` refused `https://pypi.org/project/prevalence-kit/`: C-20 allowlists LinkedIn, GitHub, GHCR and this site's own origin. The gate was right and was not bypassed. "Published on PyPI as prevalence-kit" stays in `verified_metrics`, which is how AnalystKit already states the same fact | Widening the C-20 allowlist | C-20 is a charter condition; widening it is a charter amendment and the owner's ruling, not a side effect of adding an entry. Named as a carried obligation instead | `data/projects.json`, STEP-13 §7 |
| 5.9.8 | **Defect, found in-phase and fixed: the coordinate test was passing vacuously.** `tests/test_chart.py` parsed attributes with `([a-z-]+)="..."`, which matches no name containing a digit — so `x1`, `y1`, `x2`, `y2`, `cx` and `cy` were dropped from every line and circle, and the test that exists to prove no coordinate moved was comparing elements with no coordinates in them. Caught by its own negative control, which raised `StopIteration` looking for a y attribute to tamper with. Fixed to `([a-z][a-z0-9-]*)`. **This is the doctrine's own case:** the check and the thing it checks were written from one understanding, and only the deliberate attempt to make it fail exposed that it could not | Removing the negative control as awkward | A negative control that fires is the control working. Recorded here because the near-miss is the lesson, not the regex | `tests/test_chart.py` |
| 5.9.9 | **Observation, not defect: `build.py`'s colour-literal gate does not recurse.** It globs `TPL.glob("*.j2")` (`build.py:508`), so templates under `templates/diagrams/` and `templates/figures/` are outside it. No literal is present in either directory today — `gen_diagrams.py` and `gen_chart.py` both emit classes — so this is a latent gap, not a live one. `test_no_colour_literal_survives_in_any_figure` closes it for `figures/`; whether `build.py` itself starts recursing is left to a ruling, in the manner of CU-2 | Widening the gate in this phase | Out of STEP-13's scope, which names one data file, one generator, one template and one CSS block. Named rather than silently fixed or silently left | `tests/test_chart.py`, STEP-13 §7 |

**Reading R-P5.9-1.** Contract 3.2 bars type-step literals outside `tokens.css`, and the adapted SVG keeps the source's `font-size="16"`, `"12"` and `"11"`. Read as: a font-size inside a viewBox is a drawing coordinate in the artifact's own space, scaled by the browser to whatever width the figure is given, not a CSS type step. Rewriting those to this site's scale would redraw the chart, which the owner ruled against. Recorded so the exception is a reading, not an oversight.

## Content updates (after P4.2, SOP section 1)

Not a phase. Recorded here because a content update extended a closed set that a gate guards.

| # | Decision | Alternative(s) not taken | Reason | Recorded in |
|---|---|---|---|---|
| CU-1 | **Fourth metrics basis, REPO-STATED, defined 2026-08-22 by owner's ruling: the figure is the source repository's own published count, adopted with its stated definition and anchored ref. First use: switchyard, 658 tests, 0 failing, README.md:22 and :218 at v1.0.0 = 560c107, defined at :223 as passed plus environment-dependent skips (55 integration, 6 cluster).** The earlier owner-run exit-checklist figure of 649 passed / 0 skipped is superseded and does not publish; reconciling the two runs is a switchyard-repo follow-up. Admitted to `tests/test_gates.py::test_metrics_basis_agrees_with_entries` (allowed set and the two-way clause check), proven to refuse in both directions before commit | (b) Label it owner-measured: misstates how the number was obtained. (c) Keep 649 owner-measured: contradicts the repo's own checked count | The basis taxonomy is a closed set guarded by test_metrics_basis_agrees_with_entries, and extending a closed set requires a recorded ruling. The gate's refusal of the undefined string was correct and stays correct for any future undefined basis | Owner's ruling, 2026-08-22; `data/projects.json` `_metrics_basis` clause (4) |
| CU-2 | **Observation, not defect: C-27's em-dash gate is scoped to `experience.json` only** (`tools/check_content.py:5`). `projects.json` was hand-checked this run; the new switchyard text carries no em-dash. Whether the gate widens is left to a future ruling | Widening the gate in this run | Out of this run's scope, which is one data file plus the generated anchor, widened by ruling to one test file and this entry | Owner's ruling, 2026-08-22 |
| CU-3 | **Defect, witnessed: `tools/fetch_stats.py` `repo_names()` assumed every GitHub link is a repo root.** Exposed 2026-08-22 by the first deep links in the record, switchyard's `see_it_running` (#fragment) and `showcase` (/blob/ path): the whole remainder after the owner prefix became a repo name, urllib dropped the fragment, and `releases/latest` answered with repo metadata, so the fetcher died with `KeyError: 'tag_name'`. It would have crashed `refresh-stats.yml` on its next scheduled run. Found before push, because a rebase conflict forced a regeneration. Fixed by `repo_name_from_link()`: first path segment only, fragments and query dropped; covered by `tests/test_fetch_stats.py` with a positive control (three switchyard links yield one name) and a negative control (non-GitHub links yield none). The new tests could not be run against the old code as written, since they call a signature it did not have; the defect was instead reproduced by running the old function from `HEAD` against the real data file, which returned the two bogus names. **Builder's process note, named because this is where the lesson lives:** the rebase script ran the fetcher with its output suppressed, did not read its exit code, and staged the still-conflicted file on top of its failure, so a local commit briefly carried conflict markers. That is the D-39 shape: judging a step by the absence of visible failure rather than by its exit code. Never pushed; commit rewritten | Dropping the deep links (fixing the content around the tool) | The links are correct content and the tool's assumption was always latent; fix the tool, not the content | Owner's ruling, 2026-08-22; `tools/fetch_stats.py`, `tests/test_fetch_stats.py` |
| CU-4 | **TS-Sentry entry re-sourced to its own record and moved to REPO-STATED, 2026-08-22.** Prose now from README @ origin/main db7de24 (problem :26-27, method :10/:12-14/:16-19/:21-22/:46-47, collaboration :233-235; markup, links and bold dropped, no em-dash). Test count from the CI log, not the README: `ci.yml` job `lint-type-test` at the v1.0.0 commit c088030 (run 30718834429) reads `1229 passed, 1 skipped`, identical at fdaef60 and at db7de24; the README's '1,230 passing' is therefore the collected count, and the tag-time README's '1,228' was stale against its own CI. Coverage 93.16% and mypy on 164 files are README :143-144, identical at both refs, not re-derived here. OWNER-MEASURED retired, no entry declares it; `_metrics_basis` says so rather than deleting the clause. `_note` deleted and `_status` given a dated CLOSED clause: the 2026-08-05 open exception is discharged. Link `examples` added (tree URL, HTTP 200) | (b) 1,228 from the tag-time README; (c) keep the 2026-08-06 owner-measured figures unsourced | CI at the anchored commit is the strongest evidence available and is citable by run URL; a figure whose source is a README line that CI contradicts would be the D-37 shape | Owner's ruling, 2026-08-22; `data/projects.json` |
| CU-5 | **finding-bridge added as the seventh entry and fourth flagship, REPO-STATED, 2026-08-25, on the owner's direction that the content be the repo's README and nothing else: no rephrase, no rewrite.** Source: README.md at origin/master 41a33ed, identical to the working tree at C:\Users\mohds\finding-bridge (clean, `git status`). Problem :16-18 and :20; method :13-14, :16, :41-52, :54-56; metrics :286-288, :235-237, :266-267, :262-263 plus two badge labels (:6, :7); collaboration :228-229 and :283-285. Markup, links, code ticks and bold dropped; sentences joined; no word changed, no em-dash (README carries none). Anchor: release v1.0.0 via `tools/fetch_stats.py` (pushed_at 2026-08-25T13:38:24Z); the prose is cited at master because the Honest numbers section was updated after the tag (`b3f11b1`, `41a33ed`). The repo publishes no stats.json, so CI-MEASURED is a follow-up in that repo, as for TS-Sentry. Links: repo, container (GHCR), examples, showcase, usage; each answered HTTP 200 on 2026-08-25. Six checks exit 0 | (b) Summarise the README in the site's own words: forbidden by the owner's instruction for this update. (c) Cite the tag-time README: its test figures predate the post-release closure phase and the tree at master wins, by the README's own rule (:283-284) | The owner's instruction fixes the source; REPO-STATED is the basis whose definition (CU-1) matches a README-published figure at an anchored ref | Owner's direction, 2026-08-25; `data/projects.json` |
