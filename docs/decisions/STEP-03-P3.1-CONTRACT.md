# STEP-03 (P3.1): Scaffold, build pipeline, fonts, tokens, repo connection

**Project:** mohdsaifhussain.github.io | **Phase:** P3.1, 1 of 5 | **Date:** 2026-08-06
**Status:** Specified, not started
**Tier:** FULL (director's ruling, this session)
**Depends on:**
- STEP-01-CHARTER.md v1.0 (frozen; 36 conditions; the `_status`/C-27 verification regime; §7 platform constraint register)
- STEP-02-HANDOFF.md ("Ledger Amber"; §1 token table — the exact colour values, type steps and spacing this phase encodes; §5 the closed 3-animation list; §6 build notes)
- `data/*.json` as owner-verified 2026-08-05 at Phase 1 close
- BUILD_KICKOFF.md §1 (phase scope) and §0 (pre-flight)

**Numbering scheme — declared once here, governs the whole build** (director's ruling, 2026-08-06):

| Scheme | Refers to | Example |
|---|---|---|
| `STEP-NN` | files in `docs/decisions/` | `STEP-03-P3.1-CONTRACT.md` |
| `P3.x` | build phases | `P3.1` … `P3.5` |
| charter §8 "Phase 3" | the whole Claude Code build, all five phases | — |

Both schemes are kept deliberately; no restatement of prior documents is required.

**Standing rule:** every implementation follows the top applicable standard for what is being built. Each requirement below names its governing standard.

---

## 1. Objective

Stand up the deterministic render pipeline the whole site is generated by: data in, tokens applied, HTML out, with self-hosted subset fonts and a CI workflow that publishes to GitHub Pages. No page content is wired in this phase — the pipeline is the deliverable, not the site.

**Exit criterion:** `python build.py` renders a skeleton of all five pages from `data/*.json` with zero third-party origins in the output and zero colour literals outside `tokens.css`, the shipped font subsets are proven to carry every glyph the design uses, and the deployed URL serves that output over HTTPS.

## 2. Deliverables

| ID | Deliverable | Governing standard(s) |
|---|---|---|
| D1 | Repo scaffold, `.gitignore`, and the D-01 commit identity set globally before the first commit | C-33 (no email anywhere, incl. commit-published), C-32 (public repo is inspectable evidence) |
| D2 | `build.py` + Jinja2 template skeleton (`base.html.j2` + 5 page stubs) rendering from `data/*.json` to `_site/` | C-34 (single source of truth; no content hand-duplicated), Jinja2 3.1 official docs (cited by URL in DECISIONS) |
| D3 | `static/css/tokens.css` — every STEP-02 §1 token as a CSS custom property | STEP-02 §1 table, verbatim values; C-09 (contrast) |
| D4 | Font pipeline: official OFL sources → subset → self-hosted woff2 + **glyph-coverage report** | C-04 (zero render-blocking third-party; self-hosted), C-03 (≤500 KB first view), SIL OFL, fontTools official docs |
| D5 | `_status` rewrite on the four data files per the director's C-27 ruling (§6 below) | C-27 (metrics trace to a verifiable source; owner review) |
| D6 | GitHub Actions workflow: build + deploy to Pages | C-17 (HTTPS), GitHub Pages-via-Actions official docs (cited by URL) |
| D7 | `docs/DECISIONS.md` (preamble + P3.1 table + unexamined-defaults table) and `docs/DEFECTS.md` (D-01…D-09 from the reading pass) | C-31 (governed method, decisions logged, never silently edited) |
| D8 | First push to the existing empty public repo — **gated on the director's explicit go** | C-32; doctrine rule 6 |

## 3. Requirements

- **3.1** The D-01 identity is set **before the first commit exists**: `git config --global user.email "263689115+MohdSaifHussain@users.noreply.github.com"`, `user.name` unchanged. *Standard: C-33.* Commit authorship is immutable once pushed; this ordering is not negotiable within the phase.
- **3.2** No colour, type-step, or spacing literal appears anywhere except `tokens.css`. Templates and `site.css` reference custom properties only. *Standard: STEP-02 §1.*
- **3.3** Shipped HTML contains **zero inline `<script>` bodies and zero `style=` attributes**, from the first template onward, so the P3.4 CSP refactor is a no-op rather than a rewrite. *Standard: C-18, decision D-05.*
- **3.4** Fonts are fetched from their official upstreams, cited by URL in `DECISIONS.md`, never from a CDN or a memory of a URL. *Standard: doctrine rule 3; C-04.*
- **3.5** The subsetting step emits a **coverage report proving the presence of every codepoint the design uses**: `✓ U+2713`, `✗ U+2717`, `● U+25CF`, `↗ U+2197`, `· U+00B7`, `— U+2014`, `≥ U+2265`, `× U+00D7`, plus Latin. A missing glyph silently falls back to a system font and breaks the motif — so absence is a logged defect with a decided fallback, not a shrug. *Standard: C-04, STEP-02 §2 (motif).*
- **3.6** `build.py` fails loudly with a **distinct reason code per failure mode** (missing data file, malformed JSON, unknown token reference, missing template). *Standard: doctrine rule 5.*
- **3.7** `build.py` is deterministic: the same inputs produce byte-identical `_site/` output. *Standard: C-34; verified by double-run hash comparison.*
- **3.8** Every command written into this contract or the SOP is PowerShell 5.1-safe — `;` and `if ($?)`, never `&&`. *Standard: C-36, charter §8.*
- **3.9** `DECISIONS.md` records the **D-02 deviation from the frozen handoff** (version anchors + evidence links change §3's truth strip and §4's card) as a deviation with the ruling that authorised it. *Standard: doctrine rule 10; charter §8 amendment rule.*

## 4. Out of scope

Page content wiring and real copy (P3.2) · responsive derivation (P3.2) · metadata, OG image, favicons, sitemap, JSON-LD (P3.2) · `fetch_stats.py` and count anchoring (P3.2) · the three animations, reduced motion, IST clock (P3.3) · CSP meta, keyboard/NVDA/contrast passes (P3.4) · `/audit` content, Lighthouse/axe/validator wiring, SOP.md, the tag (P3.5) · GoatCounter (ruled out of v1.0.0 entirely by D-03).

**Not measured this phase:** Lighthouse. `font-display: swap` is retained provisionally; the handoff bans it only *if it costs a point*, and that is unmeasurable until P3.5 has the tooling. Carried as O-8 rather than decided blind.

## 4a. Review stop

**Halt after D1–D5, before D6 and D8.** Two reasons the stop falls exactly there:

1. **Something becomes immutable at D8.** Once the first commit is pushed to a public repo, its author field cannot be corrected without a force-push over published history. Template 4's rule — stop before the thing sets, not after — puts the stop before the push, not after it.
2. D2–D4 are load-bearing: every page in every later phase is generated by that pipeline and rendered in those fonts. D6/D8 are delivery. A pipeline defect found after four phases of content sit on top of it is found too late.

At the stop I will prove, by quoting exact lines with file and line numbers: the reason codes of 3.6 firing; the determinism of 3.7; the glyph coverage of 3.5; and that no literal escapes `tokens.css` per 3.2.

## 5. Exit checklist

- [ ] `python build.py` exits 0 and writes all five page stubs to `_site/`
- [ ] **Negative path:** `python build.py --selftest` on a poisoned data fixture exits non-zero and prints a *distinct* reason code per failure mode (3.6)
- [ ] **Positive control:** the same selftest on a clean fixture exits 0 — proving the check does not simply refuse everything
- [ ] Determinism: two consecutive builds produce identical output hashes (3.7)
- [ ] Glyph-coverage report shows every codepoint in 3.5 present in the shipped subsets, or names the gap and its decided fallback
- [ ] `Select-String` over `_site\*.html` finds **no** third-party origin (C-04)
- [ ] `Select-String` over `_site\*.html` finds **no** inline script body and **no** `style=` attribute (3.3)
- [ ] No colour literal outside `tokens.css` (3.2)
- [ ] `git log -1 --format='%ae'` shows the noreply address, on the *first* commit (3.1)
- [ ] `_site/` total byte count recorded as a C-03 baseline — recorded, not yet claimed as met
- [ ] `docs/DECISIONS.md` and `docs/DEFECTS.md` exist, with D-01…D-09 and the D-02 deviation entry
- [ ] **Director's action, then C-17:** after the first push, director sets Pages Source = GitHub Actions and Enforce HTTPS; the deployed URL is then confirmed to serve over HTTPS

## 6. The `_status` rewrite (D5) — exact proposed text, for approval before writing

Per your ruling: verified 2026-08-05 by owner, named exceptions listed, superseded fields pending re-verification.

**`data/profile.json`**
> `"_status": "Owner-verified 2026-08-05 at Phase 1 close (chat record), covering the fields as they stood on that date. No open exceptions. Any later edit is new text and requires owner re-verification before C-27 reports MET."`

**`data/experience.json`**
> `"_status": "Owner-verified 2026-08-05 at Phase 1 close (chat record), covering the fields as they stood on that date. No open exceptions. Must never contradict the web-resume PDF (C-29); re-checked at every release. Any later edit is new text and requires owner re-verification before C-27 reports MET."`

**`data/projects.json`**
> `"_status": "Owner-verified 2026-08-05 at Phase 1 close (chat record), covering the fields as they stood on that date. OPEN EXCEPTION: TS-Sentry metrics unverified — renders as an honest ✗ pending owner supply. SUPERSEDED FIELDS: the promise strings and per-count version anchoring rewritten under decision D-02 are new text, pending owner re-verification at the P3.2 review stop. C-27 does not report MET until that re-verification is recorded."`

**`data/certifications.json`**
> `"_status": "Owner-verified 2026-08-05 at Phase 1 close (chat record) as an empty set — the resume lists no certifications or completed courses. OPEN EXCEPTION: 00 entries, carried as a named debt and rendered as the designed empty state. Only entries with a real verification URL, or an honest no-public-verification flag, are accepted (C-27)."`

## 7. Obligations carried into P3.1

| # | Obligation | Owner | State |
|---|---|---|---|
| O-1 | TS-Sentry metrics | owner → P3.5 | open; honest ✗ |
| O-2 | Certifications entries | owner → P3.5 | open; `00 ENTRIES` empty state |
| O-3 | C-27 re-verification of D-02-rewritten fields | director → **P3.2 review stop** | **new this session**; gates C-27 |
| O-5a | Pages Source = GitHub Actions | director | **Discharged 2026-08-06.** Director set Source = GitHub Actions and deliberately selected **no** suggested workflow template — the repo uses only the D6 workflow written in this phase |
| O-5b | Enforce HTTPS ticked | director | **Ticked 2026-08-06, before the first deploy** (director's correction — the checkbox *was* available pre-deploy, reversing what this row said earlier). **C-17 does not close on the tick**: it closes on observed evidence that `http://` redirects to `https://` after the first successful deploy, plus the director's re-confirmation that the box is still ticked once the deploy is green |
| O-7 | `fonttools` + `brotli` installed and pinned | P3.1 | approved by director; versions recorded in DECISIONS |
| O-8 | `font-display: swap` measured against Lighthouse | P3.5 | **Ruled 2026-08-06:** retain `swap` provisionally, carry the measurement as a named debt. Do not drop it blind |

## 8. Honest limits entering this phase

- The site cannot report C-01/C-02/C-03 at all until P3.5 has measurement tooling. Byte counts recorded in P3.1 are baselines, not claims.
- C-17 cannot close on builder action alone; it depends on a director-only settings change.
- The D-01 fix governs commits from this point forward on this machine. It does not alter authorship already published in other repos; C-33 governs this site only.
