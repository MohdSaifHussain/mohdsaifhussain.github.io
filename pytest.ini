[pytest]
pythonpath = . tools
testpaths = tests
# -rs always reports skip REASONS. Defect D-32 was a test that stopped
# asserting anything and passed silently; a skipped test whose reason is
# invisible is the same failure wearing a different hat.
addopts = -q -rs
