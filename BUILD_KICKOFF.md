# BUILD KICKOFF — Phase 3, governed build in Claude Code
*Companion to SITE_CHARTER.md v1.0 (frozen) and Handoff.md ("Ledger Amber"). Charter wins every conflict; Handoff wins every visual question; this file is logistics.*

## 0 · Pre-flight (before opening Claude Code)

Assemble one local project folder, e.g. `C:\Users\mohds\mohdsaifhussain.github.io\`, containing:

```
docs/decisions/STEP-01-CHARTER.md      <- SITE_CHARTER.md, copied verbatim
docs/decisions/STEP-02-HANDOFF.md      <- Handoff.md, copied verbatim
data/profile.json                      <- approved Phase 1 files
data/projects.json
data/experience.json
data/certifications.json
assets/img/                            <- photo assets (unused in committed direction; kept per handoff)
assets/resume/MohdSaifHussain_Resume_Web.pdf
reference/design.html                  <- export from Claude Design: the standalone HTML of
                                          "Home Explorations.dc.html" (Export -> standalone HTML
                                          or Save as folder). Reference only — never shipped.
```

Accounts/settings needed once:
- GitHub repo **`MohdSaifHussain/mohdsaifhussain.github.io`** — public. This exact name makes it the root user site. Settings -> Pages -> deploy from GitHub Actions. Settings -> Enforce HTTPS (C-17).
- GoatCounter (goatcounter.com) — free signup for the visit counter (C-22). Until created, footer legitimately shows "VISITS —" per handoff.
- Fonts: download Instrument Serif (400 + italic) and IBM Plex Mono (400/500/600) — both open (OFL), self-host as woff2, subset per handoff build notes.

## 1 · Build phases (each = fresh session, closed against its conditions, PowerShell-safe close commands)

| Phase | Scope | Conditions closed |
|---|---|---|
| P3.1 | Repo scaffold, Python build script (Jinja2 templates <- data/*.json), GitHub Actions workflow (build + deploy to Pages), fonts subset + self-hosted, tokens as CSS custom properties | C-04, C-17, C-31 partial, C-34 |
| P3.2 | All 5 pages wired from data, **responsive derivation** (mobile layouts derived from Handoff tokens — explicit review step; slider, tables, and grids restack; type scale steps down; no horizontal overflow), semantic landmarks, metadata/OG/JSON-LD/sitemap/robots | C-05, C-23–C-26, C-33 (grep), C-35 |
| P3.3 | The three animations exactly as specced (A3.1–A3.3), reduced-motion kill switch, truth-strip clock, interaction-QA pass on every state | C-10, C-12–C-16 |
| P3.4 | Hardening: CSP meta + zero-inline-script refactor if needed, SRI/no-third-party audit, GoatCounter wired (or "—"), keyboard pass, NVDA smoke test, contrast verification | C-06–C-09, C-11, C-18–C-22 |
| P3.5 | /audit page generated by CI (Lighthouse CI, axe, validator wired into the Action; scores written to audit data — never hand-entered), SOP.md written **and each procedure performed once**, release checklist, tag v1.0.0 | C-01–C-03, C-27–C-30, C-32, C-36 |

Standing debts to carry as honest ✗ until owner supplies: TS-Sentry metrics; certifications entries.

## 2 · Kickoff prompt for Claude Code (paste as first message, from inside the project folder)

> This is a governed build. Read `docs/decisions/STEP-01-CHARTER.md` (frozen — 36 conditions, wins every conflict) and `docs/decisions/STEP-02-HANDOFF.md` (the committed design: tokens, layouts, component specs, and the complete 3-animation list — nothing else ships). `reference/design.html` is visual reference only, never shipped code. Propose a tier and a phase plan mapped to BUILD_KICKOFF.md §1, then stop for my approval before writing anything. Rules: every condition closes with evidence; defects are logged honestly, including defects found by reading; all stats render from `data/*.json` with build-time fetch per C-35 — nothing hand-entered that a script should write; interaction-QA rule from the handoff applies to every component in every state; phase-close verification commands must be PowerShell-compatible; environment is Windows, Python 3.14, git via cmd.

## 3 · Release gate (P3.5, before tag)

No release with an UNMET condition unless declared on /audit as a known limitation with reason (charter §8). Expected honest declarations at v1.0.0: HSTS-preload/X-Frame-Options unavailable on Pages (charter §7); TS-Sentry metrics pending; certifications 00 entries; visit counter "—" if GoatCounter not yet created.
